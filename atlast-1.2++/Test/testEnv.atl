
\ Run atlast fom fish like this:
\   FRED="test" atlast -v
\
\ Then when atlast is running

: test
	"REDIS_IP" getenv
	0= if
		pad type cr
	else
		"getenv failed\n" type
	then

	"REDIS_PORT" getenv
	0= if
		pad strint nip . cr
	else 
		"ip getenv failed\n" type
	then
;

test


